/**
 * This file provides environment variables specifically for Sanity Studio
 * It uses hardcoded values to ensure they're included in the build
 */

// These values are hardcoded to ensure they're included in the Sanity Studio build
export const studioConfig = {
  projectId: 'hcajm3fl',
  dataset: 'production',
  apiVersion: '2025-04-01'
}
